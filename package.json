{
  "name": "fuel-control",
  "private": true,
  "workspaces": ["services/*"],
  "scripts": {
    "eureka:up": "docker compose up -d eureka",
    "eureka:down": "docker compose down",
    "dev": "npm run eureka:up && concurrently -k -n GATEWAY,DRV1,DRV2 \"npm --workspace services/api-gateway run start:dev\" \"PORT=50051 npm --workspace services/drivers run start:dev\" \"PORT=50052 npm --workspace services/drivers run start:dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
